# Day 14 - map() and filter() with Lambda

## Topics Covered

- `map()`
- `filter()`
- Using `map()` with lambda
- Using `filter()` with lambda
- Converting a map or filter result using `list()`
- Combining `filter()` and `map()`
- Order of operations: filter → map and map → filter

## map()

`map()` applies a function to every item in a collection.

Syntax:

```python
map(function, collection)