# Day 13 - Lambda Functions

## Topics Covered

- Lambda functions
- Lambda with one parameter
- Lambda with multiple parameters
- Lambda returning Boolean values
- Lambda with `abs()`
- Using lambda with `sorted()`
- Sorting dictionaries using `key=lambda`
- Reverse sorting with `reverse=True`

## Lambda Functions

A lambda function is a small function written in one line.

Syntax:

```python
lambda parameters: expression